interface Teacher {
    firstName: String;
    lastName: String;
    fullTimeEmployee: boolean = false;
    yearsOfExperience: number;
    location: String;
    contract: Boolean;
}

interface Directors extends Teacher {
    numberOfReports: Number;
}

function printTeacher(firstName: String, lastName: String) {
    // returns the first letter of the firstName and the full lastName
    
}

interface printTeacher {
    
}

class StudentClass {
    constructor(
    public fullName: string,
    public lastName: string,
    ) {

    }
    workOnHomework() {
        return CurrentlyWorking: String;
    }
    displayName() {
        return StudentClass.firstName;
    }
}

// constructor of the class should be described through an interface 
// the class should be described through an interface
